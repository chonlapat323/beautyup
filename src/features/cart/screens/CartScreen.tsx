import { PlaceholderScreen } from "@/components/ui/PlaceholderScreen";

export function CartScreen() {
  return (
    <PlaceholderScreen
      title="Shopping cart"
      subtitle="Cart, quantity changes, and summary blocks will be connected next."
    />
  );
}
